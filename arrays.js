const students = ['Ebun', 'Edosa', 'Nic', 'Asabe', 'Uche']

const numbers = []
for(let index = 1; index <= 100; index++) {
    numbers.push = index
}
//console.log(numbers)
even_numbers = []
odd_numbers = []
for(let index = 0, index <   numbers.length; index++)
